/* eslint-disable @typescript-eslint/naming-convention */

import { P } from 'ts-pattern';

export const nullish = P.nullish;
export const nonNullable = P.nonNullable;
export const number = P.number;
export const string = P.string;
